<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bkd06k11">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BKD06K11">
        <activity
            android:name=".activities.ActivitySendBroadcast"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>

        </activity>
        <activity
            android:name=".activities.ActivityGetBroadCast"
            android:exported="false">
        </activity>

        <receiver
            android:exported="false"
            android:name=".activities.MyBroadCast">
            <intent-filter>
                <action android:name="com.example.bkd06k11.SEND_FULL_NAME">

                </action>
            </intent-filter>
        </receiver>
    </application>

</manifest>